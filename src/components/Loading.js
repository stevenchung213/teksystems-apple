import React from 'react';

const Loading = () => {
  return (
    <div style={{display: 'flex', flexDirection: 'row', margin: 'auto'}}>
      <h2 style={{color: "ghostwhite", width: '100%', textAlign: 'center', margin: '25vh 25vw'}}>Loading...</h2>
    </div>
  );
};

export default Loading;
